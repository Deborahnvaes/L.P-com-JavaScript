/* Em JS é representado por um par de {}, 
ao inves de uma extrutura indexado, ele é organizado a partir de chave e valor e o valor é um valor textual
Por Exemplo: {
(atributo) -> Nome: (valor)"Léo",
(atributo) -> Idade: 30,
(atributo) -> End: "Rua...", } 
Aqui temos 3 pares!
O identificador e um valor identificando o valor.
*/

let x = 3; //numero inteiro
let y = []; //array
let a = {}; // objeto
 // para verificar se é um array 
console.log(Array.isArray(y));
 // para saber se é um objeto
 console.log(typeof a);

const produto = {
    nome: 'Caneta',
    preco: 9.99,
    desconto: 0.2
}
// para acessar o objeto
console.log(produto.nome);
console.log(produto.preco);
console.log(produto.desconto);













